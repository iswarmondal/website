# Design system

Canonical brand guide (raw markdown, networked like `llms.txt`):

| | |
|--|--|
| **In repo** | [`public/design.md`](./public/design.md) |
| **On the web** | https://iswar.me/design.md |

Edit **`public/design.md` only**. Astro serves files in `public/` as-is, so that path is `/design.md`.

Agents changing this site’s UI must keep `public/design.md` updated (see `.cursor/rules/design-system-sync.mdc`).

---
// Legacy project case-study URLs → blog archive (301).
export function getStaticPaths() {
	return [
		{ params: { slug: 'vintage-blog-platform' } },
		{ params: { slug: 'kawaii-kiwi' } },
	];
}

return Astro.redirect('/blog/', 301);
---
